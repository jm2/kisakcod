// Census-only stub (scripts/ci/native64_census.py); never used by a real build.
// Stands in for the header scripts/increment_build.sh generates.
#pragma once
#define BUILD_NUMBER 0
char *getBuildNumber();
int getBuildNumberAsInt();
